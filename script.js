async function loadStoryCards(){const container=document.querySelector("[data-story-cards]");if(!container)return;try{const response=await fetch("assets/data/episodes.json");const stories=await response.json();container.innerHTML=stories.map(story=>`<article class="memory-card"><a href="stories/${story.slug}.html"><div class="image-wrap"><img src="${story.thumb}" alt="${story.title}"></div><div class="content"><p class="kicker">${story.country} · ${story.location}</p><h3>${story.title}</h3><div class="tags">${story.emotions.map(tag=>`<span class="tag">${tag}</span>`).join("")}</div><p>${story.summary}</p></div></a></article>`).join("")}catch(error){container.innerHTML="<p>Stories are being prepared.</p>"}}loadStoryCards();
